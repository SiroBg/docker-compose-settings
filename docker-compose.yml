services:
  nginx:
    image: nginx:latest
    ports:
          - "8080:80"
    volumes:
        - ./nginx/nginx.conf:/etc/nginx/nginx.conf
        - ./src:/var/www/html
  php:
    image: php:8.3.28-fpm
    volumes:
        - ./src:/var/www/html
